script(type="text/x-template")#preloader
    .preloader(v-if="preloading" :class="{'done' : doneClass}")
        orbit-spinner(
            :animation-duration="1200"
            :size="250"
            :color="'#ea7400'"
        )
