.logo {
    height: 26px;
    color: #fff;
    font-size: 36px;
    font-weight: 900;
    line-height: .6;
    display: inline-block;

    /* @include tablets() {
        line-height: 25px;
    }
    @include phones() {
        line-height: 20px;
    } */
}